import { TaskLogger, TraceOptions } from "./taskLogger.js";
import { Span } from "@opentelemetry/api";
export declare class LoggerAPI implements TaskLogger {
    #private;
    private static _instance?;
    private constructor();
    static getInstance(): LoggerAPI;
    disable(): void;
    setGlobalTaskLogger(taskLogger: TaskLogger): boolean;
    debug(message: string, metadata?: Record<string, unknown>): void;
    log(message: string, metadata?: Record<string, unknown>): void;
    info(message: string, metadata?: Record<string, unknown>): void;
    warn(message: string, metadata?: Record<string, unknown>): void;
    error(message: string, metadata?: Record<string, unknown>): void;
    trace<T>(name: string, fn: (span: Span) => Promise<T>, options?: TraceOptions): Promise<T>;
    startSpan(name: string, options?: TraceOptions): Span;
}
