import { UsageManager } from "../usage/types.js";
import { TimeoutManager } from "./types.js";
export declare class UsageTimeoutManager implements TimeoutManager {
    private readonly usageManager;
    private _abortController;
    private _abortSignal;
    private _intervalId;
    private _listener?;
    constructor(usageManager: UsageManager);
    registerListener(listener: (timeoutInSeconds: number, elapsedTimeInSeconds: number) => void | Promise<void>): void;
    get signal(): AbortSignal | undefined;
    reset(): void;
    abortAfterTimeout(timeoutInSeconds?: number): AbortController;
}
