import { HeartbeatsManager } from "./types.js";
export declare class StandardHeartbeatsManager implements HeartbeatsManager {
    private readonly intervalInMs;
    private listener;
    private currentAbortController;
    private lastHeartbeatYieldTime;
    private lastHeartbeatDate;
    constructor(intervalInMs: number);
    registerListener(callback: (id: string) => Promise<void>): void;
    yield(): Promise<void>;
    startHeartbeat(id: string): void;
    private startHeartbeatLoop;
    stopHeartbeat(): void;
    get lastHeartbeat(): Date | undefined;
    reset(): void;
}
