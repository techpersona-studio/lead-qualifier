import { createAsyncIterableStreamFromAsyncIterable, } from "../streams/asyncIterableStream.js";
export class NoopRealtimeStreamsManager {
    pipe(key, source, options) {
        return {
            wait: () => Promise.resolve(),
            get stream() {
                return createAsyncIterableStreamFromAsyncIterable(source);
            },
        };
    }
    async append(key, part, options) { }
}
//# sourceMappingURL=noopManager.js.map