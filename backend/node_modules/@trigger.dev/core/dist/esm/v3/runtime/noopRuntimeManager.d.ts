import { BatchTaskRunExecutionResult, TaskRunContext, TaskRunExecutionResult, WaitpointTokenResult } from "../schemas/index.js";
import { RuntimeManager } from "./manager.js";
export declare class NoopRuntimeManager implements RuntimeManager {
    disable(): void;
    waitForWaitpoint(params: {
        waitpointFriendlyId: string;
        finishDate?: Date;
    }): Promise<WaitpointTokenResult>;
    waitForTask(params: {
        id: string;
        ctx: TaskRunContext;
    }): Promise<TaskRunExecutionResult>;
    waitForBatch(params: {
        id: string;
        runCount: number;
        ctx: TaskRunContext;
    }): Promise<BatchTaskRunExecutionResult>;
}
